# Core dependencies (always required)
requests>=2.31.0
flask>=3.0.0
pytest>=7.4.0

# Live trading (optional - install only when ready to trade real money)
# Uncomment the line below or run: pip install py-clob-client
# py-clob-client>=0.34.0
